import React from 'react'

export default function
    () {
    handleClickButton = () => {
        alert('click me')
    }

    return (
        <div className='third'>
            <button onClick={() => this.handleClickButton()}>Click me</button>

        </div>
    )
}
